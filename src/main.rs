fn main() {
    // let api_token = std::env::var("API_TOKEN");
    // let api_token = std::env::var("API_TOKEN").expect("expected to have an API token");
    // dbg!(api_token);
    println!("Hello, world!");
    println!("{}", dbg!(2) == 2); // true
}
